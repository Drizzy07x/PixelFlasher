// Compatibility export for existing consumers. The implementation belongs to
// the Flash feature together with the route that owns its workflow state.
export { FlashWizard, type FlashPlan, type FlashPreview } from '../pages/flash/FlashWizard';
